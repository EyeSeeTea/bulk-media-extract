## 1. Preview data model

- [x] 1.1 Strengthen selected org unit name resolution so the preview summary shows the name instead of the raw id
- [x] 1.2 Update Capture link generation to the current enrollment-event route format and keep using the event org unit context
- [x] 1.3 Count only exportable files in preview totals while preserving skipped-file warnings

## 2. Preview step UI

- [x] 2.1 Make the preview table use the full available width
- [x] 2.2 Keep the compact summary and footer layout while showing the corrected org unit name and stats
- [x] 2.3 Preserve improved table/link styling with the updated Capture URL behavior

## 3. Verification

- [x] 3.1 Extend preview utility tests to cover the new Capture URL format and exportable-file counting
- [x] 3.2 Extend preview step or wizard UI tests to verify selected org unit names, full-width table behavior, and excluded skipped files in the totals
